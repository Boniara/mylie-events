<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.bavuta.mylie.database.dao.RoleMapper">
    
    <insert id="addRoleToUser">
        <![CDATA[
          INSERT INTO mylie.USER_ROLES_USERS (USER_ID, USER_ROLE_ID) VALUES (
            #{user.id},
            #{role.id}
          )
        ]]>
    </insert>
</mapper>